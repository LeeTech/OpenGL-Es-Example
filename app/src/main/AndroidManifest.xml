<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lee.opengles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SquareActivity" />
        <activity android:name=".CubeActivity" />
        <activity android:name=".ExampleActivity" />
        <activity android:name=".SimpleTextureActivity" />
        <activity android:name=".DrawPointActivity" />
        <activity android:name=".LineSegmentActivity" />
        <activity android:name=".TrianglesActivity" />
        <activity android:name=".Icosahedron" />
        <activity android:name=".IcosahedronActivity" />
        <activity android:name=".StarActivity"></activity>
    </application>

</manifest>